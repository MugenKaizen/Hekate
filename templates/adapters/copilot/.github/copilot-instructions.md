# GitHub Copilot instructions

Read root `AGENTS.md` and use it as the single portable workflow contract.
Apply `.workflow/status.yml` preflight, then lazy-load only the project context
needed by the current task.
