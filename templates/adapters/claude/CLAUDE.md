# CLAUDE.md

Read root `AGENTS.md` as the portable workflow contract and
`.workflow/status.yml` for preflight. Lazy-load other context only when needed.

Commands: `/init-workflow`, `/analyze`, and `/plan`.

Claude owns native delegation. Never use an external harness as an automatic
fallback.
