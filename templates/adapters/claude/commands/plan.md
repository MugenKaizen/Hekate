---
description: Produce a task plan when risk, size, delegation, or the user requires one
---

Produce a plan for:

$ARGUMENTS

Include context, affected paths, ordered steps, verification, and rollback only
where meaningful. Compare alternatives first only when there is a material
design choice. Return the plan in the conversation unless optional local
history is enabled and a resumable note is useful.

Do not require another approval turn unless the user requested planning before
execution or the plan materially changes agreed scope. Do not include automatic
commits or branch creation.
