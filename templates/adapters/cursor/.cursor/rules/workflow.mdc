---
description: Use AGENTS.md as the portable workflow contract
globs: **/*
alwaysApply: true
---

Read root `AGENTS.md` and apply its preflight, adaptive workflow, verification,
authorization, and scope rules. Do not duplicate those rules here. Lazy-load
`.workflow/*` project context only when the current task needs it.
