---
description: Produce a task plan when risk, size, delegation, or the user requires one
---

Produce a plan for:

$ARGUMENTS

Include affected paths, ordered steps, verification, and rollback where useful.
Compare alternatives only for a material choice. Do not add an approval turn,
commit, or branch unless the user explicitly requested it.
